---
title: Glossary
---

- **Upstream**: A backend service the gateway proxies to (e.g. trade/task).
- **Readiness**: `GET /readyz` performs upstream checks before reporting healthy.
- **Admin server**: Control-plane HTTP server (status/config/reload) typically bound to localhost.
- **Problem+JSON**: Standard error format returned by some gateway handlers.

