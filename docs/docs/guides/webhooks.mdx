---
title: Webhooks
---

Example config:

```yaml
webhooks:
  enabled: true
  endpoints:
    - name: trade-webhook
      path: /webhooks/trade
      targetURL: https://trade.example.com/webhooks
      secret: ${TRADE_WEBHOOK_SECRET}
      maxAttempts: 5
      initialBackoff: 1s
      timeout: 10s
```

The gateway validates HMAC signatures and forwards payloads with retry/backoff.

## Deep dive

- Reference: [webhooks config](../reference/config#webhooks), [Problem+JSON](../reference/problems)
- Code: [Webhook handler](../annotated/pkg/gateway/webhook/handler), [Webhook config + validation](../annotated/pkg/gateway/config/)
