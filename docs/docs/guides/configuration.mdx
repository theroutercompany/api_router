---
title: Configure Upstreams
---

Configuration is YAML-first. See `config/examples/gateway.sample.yaml` for a commented template.

Validate config:

```bash
go run ./cmd/apigw validate --config gateway.yaml
```

